# グループ判定アルゴリズム (groupPeople)

本リポジトリは、画像から検出された人物群の位置座標、骨格座標、および体の向きを利用し、行列（キュー）の存在を認識した上でグループ（横並び＋前後跨ぎの関係）を判定するアルゴリズム群を提供します。

---

## アルゴリズム構成

全体の判定ロジックは、以下の3つの独立した子アルゴリズムによって順次構成されます。

1. **`estimateQueueLine` (列推定アルゴリズム)**
   - 人物群をバウンディングボックスのX座標順に並び替え、横幅の変化傾向に反するノイズを除外した上で、主成分分析（PCA）による直線軸と横幅勾配によるベクトル方向（起点 $\boldsymbol{p}_{\text{origin}}$ および方向単位ベクトル $\boldsymbol{d}$）を算出します。

2. **`groupByDistance` (距離・方向ベースのグループ化アルゴリズム)**
   - 推定された主直線 $\boldsymbol{L}$ の法線方向（横並び方向）および進行方向への射影座標を求め、連結成分判定 (Union-Find) により初期の横並びグループ（`Groups`）を算出します。

3. **`groupByOrientation` (体の向きに基づく前後グループ結合アルゴリズム)**
   - 初期抽出された横並びグループ群に対し、隣り合う前後グループ間の人物における「体の向き（Vector）」の交差判定を行い、前後で会話をしているカップルや合流者などの前後跨ぎグループを統合して最終的なグループを算出します。