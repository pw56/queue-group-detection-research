# アプローチのメモ

1. ベースライン
2. ROI処理
3. 人物をオブジェクトではなく顔で検出
4. 待機列は等間隔で並ぶことが多い → 距離ではなく顔、体の向き(体の向きの交差の角度のような数学的な手法で判定)でグループ判定 (おそらくこれでトイレで、連れに先に並んでもらっていた場合も解決する) ※ `MediaPipe Face Landmarker`に`Transformation Matrix`というのがあるので、公式の機能で顔の向きを取得できる
5. 通行人の誤検出を避けるため前後のフレームの情報も組み合わせる
