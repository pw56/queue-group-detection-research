name: Build approach_09_optimize_performance / subapproach_01_mediapipe_refresher

on:
  push:
    paths:
      - 'src/approach_09_optimize_performance/subapproach_01_mediapipe_refresher/**'

# GitHub Pages へのデプロイに必要な権限を設定
permissions:
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    uses: ./.github/workflows/build-and-deploy.yml
    with:
      directory: ./src/approach_09_optimize_performance/subapproach_01_mediapipe_refresher
