name: Build approach_07_deduplication

on:
  push:
    paths:
      - 'src/approach_07_deduplication/**'

# GitHub Pages へのデプロイに必要な権限を設定
permissions:
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    uses: ./.github/workflows/build-and-deploy.yml
    with:
      directory: ./src/approach_07_deduplication
