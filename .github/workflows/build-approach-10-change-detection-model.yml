name: Build approach_10_change_detection_model

on:
  push:
    paths:
      - 'src/approach_10_change_detection_model/**'

# GitHub Pages へのデプロイに必要な権限を設定
permissions:
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    uses: ./.github/workflows/build-and-deploy.yml
    with:
      directory: ./src/approach_10_change_detection_model
